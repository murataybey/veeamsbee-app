services:
  sbee:
    build: .
    image: sbee-app
    container_name: sbee
    restart: unless-stopped
    ports:
      - "${SBEE_PORT:-8080}:8080"
    env_file: .env
    volumes:
      - ./data:/web/data
